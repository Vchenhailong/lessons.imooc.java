/**
 * Title: Lesson_staticClass.java<br/>
 * Description: <br/>
 *
 */
package lessons.imooc.java;

/**
 *静态方法的使用：静态成员变量只可由静态方法调用；且习惯上，使用类名调用的方式
 *
 */
public class Lesson_staticClass {

    // 定义静态变量score1
    static int score1 = 86;
    // 定义静态变量score2
    static int score2 = 92;

    // 定义静态方法sum，计算成绩总分，并返回总分
    public static int sum() {
        int sum = score1 + score2;
        return sum;

    }

    public static void main(String[] args) {

        // 调用静态方法sum并接收返回值
        int allScore = Lesson_staticClass.sum();

        System.out.println("总分：" + allScore);
    }
}
